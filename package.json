{
  "name": "event-sphere-lite-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix event-sphere-lite",
    "build": "npm run build --prefix event-sphere-lite",
    "preview": "npm run preview --prefix event-sphere-lite"
  }
}
